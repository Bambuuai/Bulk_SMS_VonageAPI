export default function Home() {

//   useEffect(() => {
//     if (accessToken) {
//       router.push('/dashboard');  // Redirect to dashboard if access token exists
//     } else {
//       router.push('/login');  // Redirect to login if no access token
//     }
//   }, [accessToken, router]);

  return <></>; // I could return a loading spinner while the redirection occurs
}
